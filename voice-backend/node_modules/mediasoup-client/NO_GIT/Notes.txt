## New release

- Update version in package.json

- Generate transpiled lib/ files:
`npm run typescript:build`

- Commit new version:
`git commit -am"3.6.6"`

- Create tag:
`git tag -a 3.6.6 -m "3.6.6"`

- Push:
  `git push origin v3 && git push origin --tags`

- Publish npm:
  `npm publish`
